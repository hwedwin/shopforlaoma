<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:share_nodes, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:share_nodes, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:share_nodes, :edit, :id => @share_node.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :share_node, url(:share_nodes, :update, :id => @share_node.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'share_nodes/form', :locals => { :f => f } %>
  <% end %>
</div>
